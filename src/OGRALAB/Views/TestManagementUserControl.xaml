<UserControl x:Class="OGRALAB.Views.TestManagementUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:OGRALAB.Converters"
             Background="#F8F9FA">

    <UserControl.Resources>
        <!-- Converters -->
        <converters:EnumToDisplayNameConverter x:Key="EnumToDisplayNameConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!-- Styles -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,10,0,15"/>
        </Style>

        <Style x:Key="SimpleButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#27AE60"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2ECC71"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#95A5A6"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SimpleDataGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#ECF0F1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Tests Section -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0" Text="إدارة التحاليل - Test Management (مضمن)" Style="{StaticResource HeaderTextStyle}"/>

            <!-- Toolbar -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="تحديث" Style="{StaticResource SimpleButtonStyle}" Command="{Binding RefreshCommand}"/>
                <Button Content="فتح النافذة الكاملة" Style="{StaticResource SimpleButtonStyle}" 
                       Command="{Binding OpenTestManagementCommand}"/>
            </StackPanel>

            <!-- Tests List -->
            <DataGrid Grid.Row="2" 
                     ItemsSource="{Binding Tests}"
                     SelectedItem="{Binding SelectedTest}"
                     Style="{StaticResource SimpleDataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Test Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="80"/>
                    <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat='{}{0:F2}'}" Width="80"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Status -->
            <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" 
                      Foreground="#7F8C8D" FontSize="10" Margin="0,5,0,0"
                      Visibility="{Binding StatusMessage, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>

        <!-- Splitter -->
        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="#BDC3C7"/>

        <!-- Reference Ranges Section -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0" Text="المعدلات الطبيعية" FontSize="16" FontWeight="Bold" 
                      Foreground="#2C3E50" Margin="0,10,0,10"/>

            <!-- Selected Test Info -->
            <Border Grid.Row="1" Background="White" BorderBrush="#BDC3C7" BorderThickness="1" 
                   Padding="10" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="{Binding SelectedTest.Name, StringFormat='التحليل المحدد: {0}'}" 
                              FontWeight="Bold" Foreground="#34495E"/>
                    <TextBlock Text="{Binding SelectedTest.Unit, StringFormat='الوحدة: {0}'}" 
                              FontSize="10" Foreground="#7F8C8D"/>
                    <TextBlock Text="{Binding ReferenceRanges.Count, StringFormat='عدد المعدلات: {0}'}" 
                              FontSize="10" Foreground="#7F8C8D"/>
                </StackPanel>
            </Border>

            <!-- Reference Ranges List -->
            <DataGrid Grid.Row="2" 
                     ItemsSource="{Binding ReferenceRanges}"
                     SelectedItem="{Binding SelectedReferenceRange}"
                     Style="{StaticResource SimpleDataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Gender" 
                                       Binding="{Binding Gender, Converter={StaticResource EnumToDisplayNameConverter}}" 
                                       Width="60"/>
                    <DataGridTextColumn Header="Age" Binding="{Binding AgeDescription}" Width="80"/>
                    <DataGridTextColumn Header="Value" Binding="{Binding ReferenceValue}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>