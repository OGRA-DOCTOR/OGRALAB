<Window x:Class="OGRALAB.Views.UserManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:OGRALAB.Converters"
        xmlns:enums="clr-namespace:OGRALAB.Enums"
        Title="User Management - إدارة المستخدمين" 
        Height="750" Width="1100"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F8F9FA"
        MinHeight="600" MinWidth="900">

    <Window.Resources>
        <!-- Converters -->
        <converters:EnumToDisplayNameConverter x:Key="EnumToDisplayNameConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!-- Styles -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,5,0,10"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#34495E"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#3498DB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#3498DB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#27AE60"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2ECC71"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#95A5A6"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5DADE2"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#95A5A6"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E74C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EC7063"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#95A5A6"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#ECF0F1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Loading Overlay -->
        <Border Background="#80000000" 
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                Panel.ZIndex="999">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="100" Height="10" Margin="10"/>
                <TextBlock Text="Loading..." Foreground="White" FontSize="14" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#34495E" Padding="15">
                <TextBlock Text="إدارة المستخدمين - User Management" 
                          Foreground="White" FontSize="18" FontWeight="Bold"/>
            </Border>

            <!-- Main Content Area -->
            <Grid Grid.Row="1" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!-- Users List -->
                <GroupBox Grid.Column="0" Header="قائمة المستخدمين - Users List" 
                         Style="{DynamicResource {x:Type GroupBox}}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Toolbar -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
                            <Button Content="تحديث - Refresh" 
                                   Style="{StaticResource SecondaryButtonStyle}"
                                   Command="{Binding RefreshCommand}"/>
                        </StackPanel>

                        <!-- DataGrid -->
                        <DataGrid Grid.Row="1" 
                                 ItemsSource="{Binding Users}"
                                 SelectedItem="{Binding SelectedUser}"
                                 Style="{StaticResource DataGridStyle}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="120"/>
                                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="150"/>
                                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                                <DataGridTextColumn Header="Role" 
                                                   Binding="{Binding Role, Converter={StaticResource EnumToDisplayNameConverter}}" 
                                                   Width="100"/>
                                <DataGridTextColumn Header="Created" 
                                                   Binding="{Binding CreatedAt, StringFormat='yyyy-MM-dd HH:mm'}" 
                                                   Width="130"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </GroupBox>

                <!-- Splitter -->
                <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="#BDC3C7"/>

                <!-- User Form -->
                <GroupBox Grid.Column="2" Header="بيانات المستخدم - User Details">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="10">

                            <!-- Basic Info -->
                            <TextBlock Text="المعلومات الأساسية - Basic Information" Style="{StaticResource HeaderTextStyle}"/>

                            <Label Content="اسم المستخدم - Username*:" Style="{StaticResource LabelStyle}"/>
                            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                                    Style="{StaticResource TextBoxStyle}"/>

                            <Label Content="الاسم الكامل - Full Name:" Style="{StaticResource LabelStyle}"/>
                            <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" 
                                    Style="{StaticResource TextBoxStyle}"/>

                            <Label Content="البريد الإلكتروني - Email:" Style="{StaticResource LabelStyle}"/>
                            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                                    Style="{StaticResource TextBoxStyle}"/>

                            <Label Content="الصلاحيات - Role:" Style="{StaticResource LabelStyle}"/>
                            <ComboBox ItemsSource="{Binding UserRoles}"
                                     SelectedItem="{Binding SelectedRole}"
                                     Style="{StaticResource ComboBoxStyle}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource EnumToDisplayNameConverter}}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>

                            <!-- Password Section -->
                            <Separator Margin="0,15,0,10"/>
                            <TextBlock Text="كلمة المرور - Password" Style="{StaticResource HeaderTextStyle}"/>

                            <Label Content="كلمة المرور - Password*:" Style="{StaticResource LabelStyle}"/>
                            <PasswordBox x:Name="PasswordBox" 
                                        Style="{StaticResource PasswordBoxStyle}"
                                        PasswordChanged="PasswordBox_PasswordChanged"/>

                            <Label Content="تأكيد كلمة المرور - Confirm Password*:" Style="{StaticResource LabelStyle}"/>
                            <PasswordBox x:Name="ConfirmPasswordBox" 
                                        Style="{StaticResource PasswordBoxStyle}"
                                        PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>

                            <!-- Password Validation Messages -->
                            <TextBlock Text="كلمة المرور يجب أن تحتوي على 6 أحرف على الأقل"
                                      Foreground="Orange" FontSize="10" Margin="5,0,5,5"
                                      Visibility="{Binding IsPasswordValid, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                            <TextBlock Text="كلمتا المرور غير متطابقتين"
                                      Foreground="Red" FontSize="10" Margin="5,0,5,5"
                                      Visibility="{Binding ArePasswordsMatching, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                            <!-- Action Buttons -->
                            <Separator Margin="0,15,0,10"/>

                            <StackPanel>
                                <Button Content="إضافة مستخدم جديد - Add New User" 
                                       Style="{StaticResource PrimaryButtonStyle}"
                                       Command="{Binding AddUserCommand}"/>

                                <Button Content="تحديث بيانات المستخدم - Update User" 
                                       Style="{StaticResource SecondaryButtonStyle}"
                                       Command="{Binding UpdateUserCommand}"/>

                                <Button Content="تغيير كلمة المرور - Change Password" 
                                       Style="{StaticResource SecondaryButtonStyle}"
                                       Command="{Binding ChangePasswordCommand}"/>

                                <Button Content="حذف المستخدم - Delete User" 
                                       Style="{StaticResource DangerButtonStyle}"
                                       Command="{Binding DeleteUserCommand}"/>

                                <Separator Margin="0,10"/>

                                <Button Content="مسح النموذج - Clear Form" 
                                       Style="{StaticResource SecondaryButtonStyle}"
                                       Command="{Binding ClearFormCommand}"/>
                            </StackPanel>

                        </StackPanel>
                    </ScrollViewer>
                </GroupBox>
            </Grid>

            <!-- Status Bar -->
            <Border Grid.Row="2" Background="#ECF0F1" Padding="10" 
                   Visibility="{Binding StatusMessage, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="{Binding StatusMessage}" 
                          Foreground="#2C3E50" FontSize="12"/>
            </Border>

            <!-- Bottom Buttons -->
            <Border Grid.Row="3" Background="#F8F9FA" Padding="10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="إغلاق - Close" 
                           Style="{StaticResource SecondaryButtonStyle}"
                           Command="{Binding CloseCommand}"/>
                </StackPanel>
            </Border>

        </Grid>
    </Grid>
</Window>