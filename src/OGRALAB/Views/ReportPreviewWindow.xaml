<Window x:Class="OGRALAB.Views.ReportPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Height="750" Width="950"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        FlowDirection="RightToLeft"
        FontFamily="Segoe UI"
        Icon="../Images/ogralab_logo.png"
        MinHeight="600" MinWidth="800">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF2C5282"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF38A169"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF2A5170"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#FFCCCCCC"/>
                                <Setter Property="Foreground" Value="#FF666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolbarButtonStyle}">
            <Setter Property="Background" Value="#FFE53E3E"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFEF5350"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- شريط الأدوات -->
            <RowDefinition Height="Auto"/>
            <!-- ملخص التقرير -->
            <RowDefinition Height="*"/>
            <!-- منطقة عرض التقرير -->
        </Grid.RowDefinitions>
        <!-- شريط الأدوات -->
        <Border Grid.Row="0" 
                Background="#FFF7FAFC" 
                BorderBrush="#FFDDDDDD" 
                BorderThickness="0,0,0,1" 
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <!-- عنوان التقرير -->
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding WindowTitle}" 
                               FontSize="16" 
                               FontWeight="Bold" 
                               Foreground="#FF2C5282"/>
                    <TextBlock Text="{Binding ReportSummary}" 
                               FontSize="12" 
                               Foreground="#FF718096" 
                               Margin="0,3,0,0"/>
                </StackPanel>
                <!-- أزرار التحكم -->
                <StackPanel Grid.Column="1" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right">
                    <Button Content="🖨️ طباعة" 
                            Command="{Binding PrintCommand}"
                            Style="{StaticResource ToolbarButtonStyle}"/>
                    <Button Content="❌ إغلاق" 
                            Command="{Binding CloseCommand}"
                            Style="{StaticResource CloseButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>
        <!-- شريط تقدم الطباعة -->
        <Border Grid.Row="1" 
                Background="#FFF0F8FF" 
                BorderBrush="#FF2C5282" 
                BorderThickness="0,0,0,1" 
                Padding="20,10"
                Visibility="{Binding IsPrinting, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" 
                             Height="4" 
                             Width="200" 
                             Margin="0,0,15,0"
                             VerticalAlignment="Center"/>
                <TextBlock Text="جاري الطباعة..." 
                           FontWeight="SemiBold" 
                           Foreground="#FF2C5282"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
        <!-- منطقة عرض التقرير -->
        <Border Grid.Row="2" 
                Background="White" 
                Margin="20"
                BorderBrush="#FFDDDDDD"
                BorderThickness="1"
                CornerRadius="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                          HorizontalScrollBarVisibility="Auto"
                          Padding="20">
                <!-- تم تصحيح هذا الجزء بإزالة الخاصية غير الصحيحة -->
                <FlowDocumentScrollViewer Document="{Binding ReportDocument}"
                                          Zoom="100"
                                          VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Auto"
                                          Background="Transparent"
                                          IsToolBarVisible="False"/>
            </ScrollViewer>
        </Border>
        <!-- شاشة التحميل العامة -->
        <Grid Grid.RowSpan="3" 
              Background="#80000000" 
              Visibility="{Binding IsPrinting, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" 
                             Height="6" 
                             Width="300" 
                             Margin="0,0,0,20"/>
                <TextBlock Text="جاري معالجة الطباعة..." 
                           Foreground="White" 
                           HorizontalAlignment="Center" 
                           FontSize="18"
                           FontWeight="SemiBold"/>
                <TextBlock Text="يرجى الانتظار..." 
                           Foreground="LightGray" 
                           HorizontalAlignment="Center" 
                           FontSize="14"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>