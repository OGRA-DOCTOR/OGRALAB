# OGRALAB - Complete Setup and Testing Instructions

## قبل البداية

تأكد من توفر المتطلبات التالية:
- Windows 10 أو 11
- .NET 8.0 SDK ([تحميل من هنا](https://dotnet.microsoft.com/download/dotnet/8.0))
- Visual Studio 2022 أو Visual Studio Code (اختياري للتطوير)

## خطوات التشغيل السريع

### 1. البناء والتشغيل باستخدام الأوامر

```bash
# فتح مجلد المشروع
cd OGRALAB

# استعادة الحزم
dotnet restore

# بناء المشروع
dotnet build

# تشغيل التطبيق
dotnet run --project src/OGRALAB
```

### 2. البناء والتشغيل باستخدام الملفات المساعدة

**للبناء:**
- انقر مرتين على `Build.bat`
- انتظر حتى اكتمال البناء

**للتشغيل:**
- انقر مرتين على `Run.bat`
- أو قم بتشغيل الأمر من Command Prompt

### 3. استخدام Visual Studio

1. افتح `OGRALAB.sln` في Visual Studio 2022
2. اختر **Build > Build Solution**
3. اضغط **F5** أو انقر **Start**

## اختبار التطبيق

### تسجيل الدخول الأولي

**بيانات المستخدم الافتراضي:**
- **اسم المستخدم:** admin
- **كلمة المرور:** Admin@123

### اختبار الوظائف

#### 1. نافذة تسجيل الدخول

**اختبار العناصر الأساسية:**
- [ ] قم بإدخال اسم مستخدم صحيح
- [ ] قم بإدخال كلمة مرور صحيحة
- [ ] جرب زر "Show Password" للكشف عن كلمة المرور
- [ ] جرب خيار "Remember Me"
- [ ] اضغط Enter للدخول
- [ ] جرب بيانات خاطئة للتأكد من رسالة الخطأ

**اختبار القائمة المنسدلة للمستخدمين:**
- [ ] قم بتسجيل الدخول بنجاح مرة واحدة
- [ ] قم بتسجيل الخروج والعودة لنافذة الدخول
- [ ] تحقق من ظهور اسم المستخدم في القائمة المنسدلة

#### 2. النافذة الرئيسية

**اختبار الوظائف:**
- [ ] تحقق من ظهور رسالة الترحيب
- [ ] تحقق من عرض اسم المستخدم في الشريط العلوي
- [ ] جرب زر "Logout" للعودة لنافذة الدخول
- [ ] جرب زر "Exit" لإغلاق التطبيق

#### 3. اختبار Single Instance

**للتحقق من منع تشغيل نسخ متعددة:**
1. قم بتشغيل التطبيق
2. حاول تشغيل نسخة أخرى من نفس التطبيق
3. يجب أن تظهر رسالة تنبيه وتنشيط النافذة الموجودة

#### 4. اختبار قاعدة البيانات

**التحقق من إنشاء قاعدة البيانات:**
- [ ] بعد أول تشغيل، تحقق من وجود مجلد `Data` في مجلد التطبيق
- [ ] تحقق من وجود ملف `ogralab.db` داخل مجلد Data
- [ ] يجب أن يحتوي على المستخدم الافتراضي admin

## اختبار السيناريوهات المختلفة

### سيناريو 1: المستخدم الجديد
1. قم بتشغيل التطبيق لأول مرة
2. استخدم البيانات الافتراضية للدخول
3. تحقق من إنشاء قاعدة البيانات تلقائياً

### سيناريو 2: تذكر المستخدم
1. قم بتفعيل "Remember Me" عند الدخول
2. أغلق التطبيق
3. أعد تشغيله وتحقق من حفظ اسم المستخدم

### سيناريو 3: أخطاء الدخول
1. أدخل اسم مستخدم خاطئ
2. أدخل كلمة مرور خاطئة
3. تحقق من ظهور رسائل الخطأ المناسبة

### سيناريو 4: إدارة الجلسة
1. قم بتسجيل الدخول
2. استخدم الوظائف المختلفة في النافذة الرئيسية
3. قم بتسجيل الخروج والتأكد من العودة لنافذة الدخول

## استكشاف الأخطاء وحلها

### مشاكل شائعة وحلولها

#### خطأ: "dotnet command not found"
**السبب:** .NET SDK غير مثبت
**الحل:** قم بتثبيت .NET 8.0 SDK من الموقع الرسمي

#### خطأ: "Could not load file or assembly"
**السبب:** مشكلة في الحزم
**الحل:** 
```bash
dotnet clean
dotnet restore
dotnet build
```

#### خطأ: "Database connection failed"
**السبب:** مشكلة في أذونات المجلد
**الحل:** تأكد من أن التطبيق لديه أذونات الكتابة في مجلد Data

#### خطأ: "Application already running"
**السبب:** هذا ليس خطأ، بل ميزة منع التشغيل المتعدد
**الحل:** أغلق النسخة الأولى من التطبيق إذا كنت تريد إعادة التشغيل

### سجلات الأخطاء

في حالة حدوث مشاكل، يمكنك فحص:
- سجلات Windows Event Viewer
- رسائل الخطأ في نافذة التطبيق
- ملفات السجل إذا تم تفعيل التسجيل

## التخصيص والإعدادات

### تعديل إعدادات قاعدة البيانات

قم بتعديل ملف `appsettings.json`:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=Data/ogralab.db"
  }
}
```

### تغيير المستخدم الافتراضي

قم بتعديل ملف `Data/OgralabDbContext.cs` في method `OnModelCreating`:

```csharp
var defaultAdmin = new User
{
    Id = 1,
    Username = "newadmin",
    PasswordHash = BCrypt.Net.BCrypt.HashPassword("NewPassword123"),
    // ... باقي الخصائص
};
```

**ملاحظة:** بعد التعديل، احذف مجلد Data واعد تشغيل التطبيق لإنشاء قاعدة بيانات جديدة.

## النشر والتوزيع

### النشر للإنتاج

```bash
# نشر مستقل (لا يحتاج .NET مثبت على الجهاز المستهدف)
dotnet publish -c Release -r win-x64 --self-contained

# نشر معتمد على الإطار (يحتاج .NET مثبت)
dotnet publish -c Release -r win-x64 --no-self-contained
```

### ملفات النشر

بعد النشر ستجد الملفات في:
```
src/OGRALAB/bin/Release/net8.0-windows/win-x64/publish/
```

**الملفات المطلوبة للتوزيع:**
- OGRALAB.exe (الملف الرئيسي)
- جميع ملفات .dll
- appsettings.json
- مجلد Images (إذا كان منفصلاً)

## دعم المراحل القادمة

### المرحلة الثانية: إدارة المرضى
سيتم إضافة:
- نوافذ إدارة المرضى
- قاعدة بيانات المرضى
- تقارير المرضى

### المرحلة الثالثة: إدارة الفحوصات
سيتم إضافة:
- أنواع الفحوصات
- طلبات الفحوصات
- نتائج الفحوصات

### التحضير للمراحل القادمة
- البنية الحالية مصممة لدعم التوسعات المستقبلية
- نظام MVVM يسهل إضافة نوافذ جديدة
- خدمات قاعدة البيانات قابلة للتطوير

## الدعم الفني

للمساعدة التقنية أو الإبلاغ عن مشاكل:
1. تحقق من دليل استكشاف الأخطاء أعلاه
2. راجع ملف README.md للمعلومات الإضافية
3. تحقق من ملف ProjectVerification.md للتأكد من اكتمال التثبيت

---

**نصائح مهمة:**
- احتفظ بنسخة احتياطية من مجلد Data قبل أي تحديثات
- قم بتشغيل التطبيق كمدير إذا واجهت مشاكل في الأذونات
- تأكد من توفر مساحة كافية على القرص الصلب لقاعدة البيانات

**تم إنجاز المرحلة الأولى بنجاح! 🎉**
